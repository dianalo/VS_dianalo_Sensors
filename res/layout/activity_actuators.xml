<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ch.ethz.inf.vs.a1.dianalo.sensors.ActuatorsActivity" >

    
<SeekBar
	android:id="@+id/seek_duration"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:max="100"
	android:progress="50"/>

<Button
    android:id="@+id/btn_sound"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignLeft="@+id/btn_vibrate"
    android:layout_below="@+id/btn_vibrate"
    android:layout_marginTop="18dp"
    android:onClick="onClickSound"
    android:text="@string/btn_stopped" />

<Button
    android:id="@+id/btn_vibrate"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignLeft="@+id/seek_duration"
    android:layout_below="@+id/seek_duration"
    android:layout_marginTop="24dp"
    android:onClick="vibrate"
    android:text="@string/btn_vibrate" />

</RelativeLayout>
